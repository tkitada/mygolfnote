<div class='container W-100 my-3'>
  <div class='row justify-content-center'>
    <div class="col-md-8">
      
      <%= form_with model: @group, url: groups_path, local: true do |f| %>
        <div class="mb-3">
          <%= f.label :name, "グループ名", class: "form-label" %>
          <%= f.text_field :name, class: "form-control" %>
        </div>

        <div class="mb-3">
          <%= f.label :introduction, "紹介文", class: "form-label" %>
          <%= f.text_area :introduction, class: "form-control" %>
        </div>

        <div class="mb-3">
          <%= f.label :group_image, "グループ画像", class: "form-label" %>
          <%= f.file_field :group_image, class: "form-control-file", accept: "image/*" %>
        </div>

        <div class="mb-3">
          <%= f.submit class: "btn btn-info" %>
        </div>
      <% end %>

      <div class="
    </div>
  </div>
</div>
  
